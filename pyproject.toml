[project]
name = "bookmarks"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "flask>=3.1,<4.0",
    "gunicorn>=20.1.0",
    "pymongo>=4.0.1",
    "requests>=2.32.5",
    "validators>=0.18.2",
]

[dependency-groups]
dev = [
    "pylint",
]
